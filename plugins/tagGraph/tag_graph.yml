name: Tag Graph
description: Creates a visual of the Tag relations
version: 0.2
exec:
  - python
  - "{pluginDir}/deps/plugin_init.py"
interface: raw
tasks:
  - name: Generate Graph
    description: generates graph from current tag data
    defaultArgs:
      mode: generate